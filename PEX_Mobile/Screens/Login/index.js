import { StyleSheet, View, Pressable, Text } from "react-native";

const LoginScreen = ({ navegation }) => {
  return (
    <View>
      <Text> Sou uma nova tela</Text>
    </View>
  );
};

export default LoginScreen;
